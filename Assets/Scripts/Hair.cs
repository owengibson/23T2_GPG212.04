using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

namespace GPG212_04
{
    public class Hair : MonoBehaviour
    {
        /*private void OnTriggerEnter2D(Collider2D collision)
        {
            Debug.Log("triggered");
            GetComponent<SpriteRenderer>().sprite = collision.GetComponent<ToolManager>().hairShaved;

            if (collision.CompareTag("Player"))
            {
                switch (collision.GetComponent<ToolManager>().currentTool)
                {
                    case Tool.Clippers:
                        
                        break;
                    case Tool.Razor:
                        break;
                    case Tool.Dye:
                        break;
                    case Tool.Formula:
                        break;
                    default:
                        break;
                }
            }
        }*/
    }
}
